/**
 * Placeholder shell. The real three-column layout arrives in Phase 6; this
 * exists so Phase 0 can prove that dev, build, and preview all work.
 */
export default function App() {
  return (
    <main className="shell">
      <h1>Mosaic</h1>
      <p>Turn a photo into a buildable LEGO brick mosaic.</p>
      <p className="muted">
        Scaffolding only — see <code>TODO.md</code> for the implementation plan.
      </p>
    </main>
  );
}
